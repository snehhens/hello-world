//c++ Program for taking input from user and add two numbers and display them 

/*#include <iostream> // Include Header File

int main() {
    // Declare variables to store the user input and the Output in sum
    int num1;
    int num2;
    int sum;
    // int num1,num2,sum;

    // Ask the user to enter the first number
    std::cout << "Hey there! I'm Guy Sneh.\n";
    std::cout << "Please enter the first number: ";
    std::cin >> num1;

    // Ask the user to enter the second number
    std::cout<<"Great! Now, please enter the second number: ";
    std::cin>>num2;

    // Calculate the sum of the two numbers
    // using operators *,/,- instade of + 
    sum = num1 + num2;

    // display the result
    std::cout << "Awesome! The sum of " << num1 << " and " << num2 << " is: " << sum << std::endl;

    // Return 0 to indicate successful program execution
    return 0;
}*/

#include <iostream> 
 using namespace std; 
int main() {
   

    int num1, num2, sum;
    cout<<"Hey there! I'm Sneh.\n";
    cout<<"Please enter the first number: ";
    cin>>num1;
    cout<<"Great! Now, please enter the second number: ";
    cin>>num2;13
    sum = num1+num2;
    cout<<"Awesome! The sum of " <<num1<<" and "<<num2<< " is: " <<sum <<endl;
    return 0;
}

